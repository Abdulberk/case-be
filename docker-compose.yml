services:
  postgres:
    image: postgres:16-alpine
    container_name: case-be-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: case_be
    ports:
      - '5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d case_be']
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres-data:
